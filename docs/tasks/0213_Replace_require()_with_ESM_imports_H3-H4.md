---
name: Replace require() with ESM imports H3-H4
description: { { DESCRIPTION } }
status: Done
created_at: 2026-03-15 22:46:15
updated_at: 2026-03-15 23:15:47
impl_progress:
  planning: completed
  design: completed
  implementation: completed
  review: completed
  testing: completed
---

## 0213. Replace require() with ESM imports H3-H4

### Background

Code review found require() usage in opencode.ts (H3) and evaluate.ts (H4) despite existing top-level imports. Parent task: 0210.

### Requirements

Replace all require() calls with top-level ESM imports. Zero require() calls remaining. Run bun test.

### Q&A

[Clarifications added during planning phase]

### Design

[Architecture/UI specs added by specialists]

### Solution


## Batch 3: Import Cleanup (H3-H4)

Replace `require()` calls with proper ESM imports.

### H3: opencode.ts uses require() instead of import
- Line 44: `require('node:fs').readFileSync` despite `existsSync` already imported
- **Fix**: Add `readFileSync` to the existing `node:fs` import

### H4: evaluate.ts uses require() inside functions
- Lines 161, 813: `runTests()` and `evaluateCodeQuality()` use `require()`
- **Fix**: Move to top-level imports

### Acceptance Criteria
- Zero `require()` calls in codebase
- All imports are top-level ESM
- `bun test` passes

### Plan

[Step-by-step implementation plan]

### Artifacts

| Type | Path | Generated By | Date |
| ---- | ---- | ------------ | ---- |

### References

[Links to docs, related tasks, external resources]
