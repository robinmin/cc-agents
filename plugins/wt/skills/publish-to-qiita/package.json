{
  "name": "publish-to-qiita-tests",
  "version": "1.0.0",
  "description": "Tests for publish-to-qiita skill",
  "type": "module",
  "scripts": {
    "test": "bun test tests/**/*.test.ts",
    "test:watch": "bun test --watch tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@wt/web-automation": "workspace:*"
  }
}
