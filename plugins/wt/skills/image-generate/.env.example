# Image Generation Environment Variables
# Copy this file to .env and fill in your API keys

# =============================================================================
# HuggingFace API (Primary Backend)
# =============================================================================

# HuggingFace API token for image generation via Inference API
# Get your token at: https://huggingface.co/settings/tokens
# Required for: huggingface backend (stable-diffusion-xl-base-1.0)
HUGGINGFACE_API_TOKEN=your_huggingface_token_here

# Optional: Override default HuggingFace model
# Default: stabilityai/stable-diffusion-xl-base-1.0
# HUGGINGFACE_MODEL=stabilityai/stable-diffusion-xl-base-1.0

# =============================================================================
# Gemini Imagen API (Fallback Backend 1)
# =============================================================================

# Google Gemini API key for Imagen image generation
# Get your API key at: https://makersuite.google.com/app/apikey
# Required for: gemini backend (imagen-3.0-generate-001)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override default Imagen model
# Default: imagen-3.0-generate-001
# Options: imagen-3.0-generate-001, imagen-4.0-generate-001 (when available)
# GEMINI_MODEL=imagen-3.0-generate-001

# =============================================================================
# nano banana / Z-Image Turbo (Fallback Backend 2)
# =============================================================================

# nano banana uses MCP HuggingFace server (mcp__huggingface__gr1_z_image_turbo_generate)
# No separate API key needed - uses HuggingFace MCP server configuration
# See: https://github.com/modelcontextprotocol/servers
#
# MCP server configuration is handled in your Claude Code settings (~/.claude/settings.json)
# Example MCP server configuration:
# {
#   "mcpServers": {
#     "huggingface": {
#       "command": "npx",
#       "args": ["-y", "@modelcontextprotocol/server-huggingface"]
#     }
#   }
# }

# =============================================================================
# Framework Configuration
# =============================================================================

# Optional: Set default backend priority
# Framework auto-selects first available backend in order:
# 1. huggingface (if HUGGINGFACE_API_TOKEN is set)
# 2. gemini (if GEMINI_API_KEY is set)
# 3. nano_banana (if MCP HuggingFace server is configured)
#
# Override with: --backend huggingface|gemini|nano_banana
# IMAGE_GENERATION_BACKEND=huggingface

# =============================================================================
# Python Package Dependencies
# =============================================================================

# Install required packages with uv:
# uv add requests Pillow    # For HuggingFace backend
# uv add google-genai       # For Gemini Imagen backend
#
# nano banana backend uses MCP tools (no additional packages needed)
