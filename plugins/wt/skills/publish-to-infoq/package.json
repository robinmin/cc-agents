{
  "name": "publish-to-infoq-tests",
  "version": "1.0.0",
  "description": "Tests for publish-to-infoq skill",
  "type": "module",
  "scripts": {
    "test": "bun test tests/**/*.test.ts",
    "test:watch": "bun test --watch tests/**/*.test.ts"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@wt/web-automation": "workspace:*"
  }
}
