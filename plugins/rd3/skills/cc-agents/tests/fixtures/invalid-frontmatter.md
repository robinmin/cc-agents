---
title: Bad Agent
flavor: chocolate
priority: 99
---

This agent has invalid frontmatter.

It is missing the required `name` and `description` fields.
It has unknown fields like `title`, `flavor`, and `priority`.
