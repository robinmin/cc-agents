{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "help": "echo 'Available scripts: install, test, test:unit, test:integration, test:e2e, test:file, test:function, lint, format, build, clean, notify:start, notify:end, stage-*'",

    "install": "pnpm install",

    "test": "npm run notify:start && jest --coverage && npm run notify:end",
    "test:unit": "jest --testPathPattern=tests/unit --coverage",
    "test:integration": "jest --testPathPattern=tests/integration --coverage",
    "test:e2e": "jest --testPathPattern=tests/e2e",
    "test:file": "jest",
    "test:function": "jest -t",
    "test:watch": "jest --watch",

    "lint": "eslint src tests --ext .ts,.tsx",
    "lint:fix": "eslint src tests --ext .ts,.tsx --fix",
    "type-check": "tsc --noEmit",

    "format": "prettier --write 'src/**/*.{ts,tsx}' 'tests/**/*.{ts,tsx}'",
    "format:check": "prettier --check 'src/**/*.{ts,tsx}' 'tests/**/*.{ts,tsx}'",

    "build": "npm run clean && npm run type-check && tsc && npm run build:bundle",
    "build:dev": "tsc --watch",
    "build:bundle": "webpack --mode production",

    "clean": "rm -rf dist coverage .eslintcache",

    "notify:start": "echo '🎼 WORKFLOW: 10-Stage Development - START' && echo Task: ${TASK_NAME:-Unknown} && echo Stage: ${STAGE:-0} && date",
    "notify:end": "echo '🎼 WORKFLOW: 10-Stage Development - COMPLETE' && echo Task: ${TASK_NAME:-Unknown} && date",

    "stage-0": "npm run notify:start",
    "stage-1": "echo '📋 Stage 1: Define Specification'",
    "stage-2": "echo '🧪 Stage 2: Create Smoke Test'",
    "stage-3": "echo '🔍 Stage 3: Initial Syntax Check' && npm run type-check && npm run lint",
    "stage-4": "echo '🧪 Stage 4: Run Smoke Test (expect failure)' && jest -t 'smoke' || true",
    "stage-5": "echo '✅ Stage 5: Implementation Complete'",
    "stage-6": "echo '🧪 Stage 6: Expand Test Suite'",
    "stage-7": "echo '🔍 Stage 7: Final Syntax Check' && npm run type-check && npm run lint",
    "stage-8": "echo '🧪 Stage 8: Verify All Tests Pass' && jest --coverage",
    "stage-9": "npm run notify:end"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.8.0",
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "@typescript-eslint/parser": "^6.7.0",
    "eslint": "^8.50.0",
    "eslint-config-prettier": "^9.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.0.3",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.2.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "src/**/*.{ts,tsx}",
      "!src/**/*.test.{ts,tsx}",
      "!src/**/__tests__/**"
    ],
    "testMatch": [
      "**/tests/**/*.test.ts",
      "**/__tests__/**/*.ts"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 80,
        "functions": 80,
        "lines": 80,
        "statements": 80
      }
    }
  }
}
